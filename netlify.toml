[build]
  command = "node netlify/inject-preview-bypass.js"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."deepseek-proxy"]
  timeout = 26

[functions."openrouter-proxy"]
  timeout = 26

[functions."firecrawl-proxy"]
  timeout = 26

[build.environment]
  NODE_VERSION = "18"
